apiVersion: v1
kind: Pod
metadata:
  name: configmap-demo
spec:
  containers:
    - name: demo-container
      image: busybox
      command: ["sh", "-c", "echo ConfigMap Vars:; env | grep APP_; sleep 3600"]
      envFrom:
        - configMapRef:
            name: app-config
